import {BrowserRouter, Routes} from "react-router-dom";

function AppRoutes() {
    return (
        <BrowserRouter>
            <Routes>

            </Routes>
        
        </BrowserRouter>
    );
}

export default AppRoutes;